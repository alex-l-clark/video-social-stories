/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove rewrites that might cause build issues
  // API routes should handle backend communication directly
};

module.exports = nextConfig;